#include <stdio.h>
#include "main.h"

/**
 * _puts_recursion - function like puts();
 * @s: input
 * Return: Always 0 (Success)
 */

int i = 0;
void _puts_recursion(char *s){
	_putchar("print a string %c\n", i);

	
		return 0;
}
